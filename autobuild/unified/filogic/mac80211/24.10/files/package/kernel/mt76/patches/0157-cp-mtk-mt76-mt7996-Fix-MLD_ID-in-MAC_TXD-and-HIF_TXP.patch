From e12305f334a2baacf4032ab7caa7abe647bb9964 Mon Sep 17 00:00:00 2001
From: Peter Chiu <chui-hao.chiu@mediatek.com>
Date: Thu, 21 May 2026 10:57:43 +0800
Subject: [PATCH] mtk: mt76: mt7996: Fix MLD_ID in MAC_TXD and HIF_TXP

Problem:
MCU command timeout and firmware state is normal.
Firmware keep showing the following error log.
"ERROR!! NO PAUSE..."

Root Cause:
If MLD_ID field in TXD is neither primary link id nor secondary
link id, it may leads to firmware busy when the third link is in
power saving mode.

Solution:
Driver fills in MLD_ID depends on TID even if driver needs to
force link on the third link.

Feature:
Stability

Signed-off-by: Peter Chiu <chui-hao.chiu@mediatek.com>
---
 mt7996/mac.c | 44 +++++++++++++++++++++++++++++++++++++-------
 1 file changed, 37 insertions(+), 7 deletions(-)

diff --git a/mt7996/mac.c b/mt7996/mac.c
index 39f34b467..bdf1eaa74 100644
--- a/mt7996/mac.c
+++ b/mt7996/mac.c
@@ -863,6 +863,31 @@ mt7996_mac_write_txwi_80211(struct mt7996_dev *dev, __le32 *txwi,
 		txwi[6] |= cpu_to_le32(MT_TXD6_DIS_MAT);
 }
 
+static struct mt76_wcid *mt7996_get_txd_wcid(struct mt76_wcid *wcid, u8 tid)
+{
+	struct mt7996_sta_link *msta_link;
+	struct mt7996_sta *msta;
+	u8 link_id;
+
+	if (!wcid->sta)
+		return wcid;
+
+	msta_link = container_of(wcid, struct mt7996_sta_link, wcid);
+	msta = msta_link->sta;
+
+	if (!msta || wcid->link_id == msta->sec_link ||
+	    wcid->link_id == msta->deflink_id)
+		return wcid;
+
+	link_id = (tid % 2) ? msta->sec_link : msta->deflink_id;
+
+	msta_link = rcu_dereference(msta->link[link_id]);
+	if (msta_link)
+		return &msta_link->wcid;
+
+	return wcid;
+}
+
 void mt7996_mac_write_txwi(struct mt7996_dev *dev, __le32 *txwi,
 			   struct sk_buff *skb, struct mt76_wcid *wcid,
 			   struct ieee80211_key_conf *key, int pid,
@@ -883,6 +908,8 @@ void mt7996_mac_write_txwi(struct mt7996_dev *dev, __le32 *txwi,
 					 BSS_CHANGED_FILS_DISCOVERY));
 	bool beacon = !!(changed & (BSS_CHANGED_BEACON |
 				    BSS_CHANGED_BEACON_ENABLED)) && (!inband_disc);
+	u8 tid = skb->priority & IEEE80211_QOS_CTL_TID_MASK;
+	struct mt76_wcid *wcid_txd;
 
 	if (wcid != &dev->mt76.global_wcid)
 		link_id = wcid->link_id;
@@ -920,7 +947,9 @@ void mt7996_mac_write_txwi(struct mt7996_dev *dev, __le32 *txwi,
 	      FIELD_PREP(MT_TXD0_Q_IDX, q_idx);
 	txwi[0] = cpu_to_le32(val);
 
-	val = FIELD_PREP(MT_TXD1_WLAN_IDX, wcid->idx) |
+	/* WLAN_IDX can only be primary or secondary link */
+	wcid_txd = mt7996_get_txd_wcid(wcid, tid);
+	val = FIELD_PREP(MT_TXD1_WLAN_IDX, wcid_txd->idx) |
 	      FIELD_PREP(MT_TXD1_OWN_MAC, omac_idx);
 
 	if (band_idx)
@@ -1026,6 +1055,7 @@ int mt7996_tx_prepare_skb(struct mt76_dev *mdev, void *txwi_ptr,
 	struct mt76_txwi_cache *t;
 	int id, i, pid, nbuf = tx_info->nbuf - 1;
 	bool is_8023 = info->flags & IEEE80211_TX_CTL_HW_80211_ENCAP;
+	u8 tid = tx_info->skb->priority & IEEE80211_QOS_CTL_TID_MASK;
 	__le32 *ptr = (__le32 *)txwi_ptr;
 	u8 *txwi = (u8 *)txwi_ptr;
 	u8 link_id;
@@ -1042,13 +1072,10 @@ int mt7996_tx_prepare_skb(struct mt76_dev *mdev, void *txwi_ptr,
 
 	msta = sta ? (struct mt7996_sta *)sta->drv_priv : &mvif->sta;
 	if ((is_8023 || ieee80211_is_data_qos(hdr->frame_control)) && sta && sta->mlo) {
-		if (unlikely(tx_info->skb->protocol == cpu_to_be16(ETH_P_PAE))) {
+		if (unlikely(tx_info->skb->protocol == cpu_to_be16(ETH_P_PAE)))
 			link_id = msta->deflink_id;
-		} else {
-			u8 tid = tx_info->skb->priority & IEEE80211_QOS_CTL_TID_MASK;
-
+		else
 			link_id = (tid % 2) ? msta->sec_link : msta->deflink_id;
-		}
 	} else {
 		link_id = u32_get_bits(info->control.flags, IEEE80211_TX_CTRL_MLO_LINK);
 
@@ -1179,6 +1206,7 @@ int mt7996_tx_prepare_skb(struct mt76_dev *mdev, void *txwi_ptr,
 	} else {
 		struct mt76_connac_txp_common *txp =
 			(struct mt76_connac_txp_common *)(txwi + MT_TXD_SIZE);
+		struct mt76_wcid *wcid_txd;
 
 		for (i = 0; i < nbuf; i++) {
 			u16 len;
@@ -1207,7 +1235,9 @@ int mt7996_tx_prepare_skb(struct mt76_dev *mdev, void *txwi_ptr,
 
 		txp->fw.bss_idx = mconf->mt76.idx;
 		txp->fw.token = cpu_to_le16(id);
-		txp->fw.rept_wds_wcid = cpu_to_le16(sta ? wcid->idx : 0xfff);
+		/* wcid in txp can only be primary or secondary link */
+		wcid_txd = mt7996_get_txd_wcid(wcid, tid);
+		txp->fw.rept_wds_wcid = cpu_to_le16(sta ? wcid_txd->idx : 0xfff);
 	}
 
 	tx_info->skb = NULL;
-- 
2.45.2

