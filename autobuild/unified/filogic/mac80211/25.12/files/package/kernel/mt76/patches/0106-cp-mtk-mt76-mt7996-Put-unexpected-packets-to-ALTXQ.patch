From 831931dd3dc59f9ad99a711b030b8fbbbc9f9853 Mon Sep 17 00:00:00 2001
From: Peter Chiu <chui-hao.chiu@mediatek.com>
Date: Thu, 4 Jun 2026 09:18:26 +0800
Subject: [PATCH] mtk: mt76: mt7996: Put unexpected packets to ALTXQ

Problem:
If AP send deauth or disassoc to peer station when
station is in power saving mode, deauth or disassoc
may queue in ACQ and lead to PLE hang.

Root Cause:
TXD cannot be transmitted because station is PSM.

Solution:
Put disassoc and packets to unauthorized stations to ALTXQ.

Feature:
TRx Path

Signed-off-by: Peter Chiu <chui-hao.chiu@mediatek.com>
---
 mt7996/mac.c  | 2 +-
 mt7996/main.c | 1 +
 tx.c          | 1 +
 3 files changed, 3 insertions(+), 1 deletion(-)

diff --git a/mt7996/mac.c b/mt7996/mac.c
index e64dfd054..e38fc152b 100644
--- a/mt7996/mac.c
+++ b/mt7996/mac.c
@@ -904,7 +904,7 @@ void mt7996_mac_write_txwi(struct mt7996_dev *dev, __le32 *txwi,
 	} else if (beacon) {
 		p_fmt = MT_TX_TYPE_FW;
 		q_idx = MT_LMAC_BCN0;
-	} else if (qid >= MT_TXQ_PSD) {
+	} else if (qid >= MT_TXQ_PSD || (!(wcid->tx_info & MT_WCID_TX_INFO_SET))) {
 		p_fmt = MT_TX_TYPE_CT;
 		q_idx = MT_LMAC_ALTX0;
 	} else {
diff --git a/mt7996/main.c b/mt7996/main.c
index 752e30662..d6bd168af 100644
--- a/mt7996/main.c
+++ b/mt7996/main.c
@@ -1696,6 +1696,7 @@ mt7996_mac_sta_event(struct mt7996_dev *dev, struct ieee80211_vif *vif,
 				goto unlock;
 			break;
 		case MT76_STA_EVENT_DISASSOC:
+			msta_link->wcid.tx_info &= ~MT_WCID_TX_INFO_SET;
 			if (links == msta->valid_links) {
 				ieee80211_iterate_stations_atomic(dev->mt76.hw, mt7996_check_mld_sta,
 								&has_mld_sta);
diff --git a/tx.c b/tx.c
index c45ebb2e5..2707717d2 100644
--- a/tx.c
+++ b/tx.c
@@ -664,6 +664,7 @@ mt76_txq_schedule_pending_wcid(struct mt76_phy *phy, struct mt76_wcid *wcid,
 		    !ieee80211_is_data(hdr->frame_control) &&
 		    (!ieee80211_is_bufferable_mmpdu(skb) ||
 		     ieee80211_is_deauth(hdr->frame_control) ||
+		     ieee80211_is_disassoc(hdr->frame_control) ||
 		     head == &wcid->tx_offchannel))
 			qid = MT_TXQ_PSD;
 
-- 
2.45.2

