From 0b9f812b7b71f179b221e9a9fb803aea34298b3b Mon Sep 17 00:00:00 2001
From: Bo Jiao <Bo.Jiao@mediatek.com>
Date: Wed, 10 Jul 2024 14:49:43 +0800
Subject: [PATCH 093/254] mtk: hostapd: fix Multiple MLDs to use the conf's
 own_addr/mld_addr.

Signed-off-by: Bo Jiao <Bo.Jiao@mediatek.com>
---
 src/ap/hostapd.c | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/src/ap/hostapd.c b/src/ap/hostapd.c
index 8a1c98d59..acea44513 100644
--- a/src/ap/hostapd.c
+++ b/src/ap/hostapd.c
@@ -1532,6 +1532,9 @@ int hostapd_setup_bss(struct hostapd_data *hapd, int first, bool start_beacon)
 				goto setup_mld;
 			}
 			use_existing = true;
+
+			if (addr && !is_zero_ether_addr(hapd->conf->mld_addr))
+				os_memcpy(addr, hapd->conf->mld_addr, ETH_ALEN);
 		}
 #endif /* CONFIG_IEEE80211BE */
 
@@ -1557,6 +1560,9 @@ int hostapd_setup_bss(struct hostapd_data *hapd, int first, bool start_beacon)
 				   hapd->mld_link_id, hapd->conf->iface);
 			os_memcpy(hapd->mld->mld_addr, hapd->own_addr,
 				  ETH_ALEN);
+
+			if (!is_zero_ether_addr(conf->bssid))
+				os_memcpy(hapd->own_addr, conf->bssid, ETH_ALEN);
 		}
 #endif /* CONFIG_IEEE80211BE */
 	}
-- 
2.45.2

